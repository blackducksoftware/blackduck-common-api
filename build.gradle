buildscript {
    repositories {
        mavenCentral()
        maven { url 'https://plugins.gradle.org/m2/' }
    }
    dependencies { classpath 'com.synopsys.integration:common-gradle-plugin:1.1.0' }
}

project.ext.moduleName = 'com.synopsys.integration.blackduck-common-api'
project.ext.javaUseAutoModuleName = 'true'

version = '2019.12.0.4'

description = 'A library of mostly generated request/response classes for the Black Duck REST API.'

apply plugin: 'com.synopsys.integration.library'

dependencies {
    api 'com.synopsys.integration:integration-rest:1.0.1'
}
